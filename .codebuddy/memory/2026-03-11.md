# 2026-03-11 工作日志

## 植物库统一重构
- 将原来分散的两个数据源（`defaultPlants` 27种 in index.html + `recommendPool` 200种 in recommend-data.js）合并为统一的 `plantDatabase`（226种去重）存储在 `plant-database.js`
- 每个植物新增字段：`isDefault`（是否默认显示）、`defaultZone`（推荐的光照区域）
- 重构持久化逻辑：
  - 旧逻辑：黑名单机制（`plant_deleted_names`）+ 添加列表（`plant_added_list`）
  - 新逻辑：直接存储区域内植物列表（`plant_zone_list`），只保存 `[{name, zone}]`
- 删除植物 = 从区域内移除（不再是永久黑名单，推荐时可再次出现）
- 推荐植物 = 从整个226种植物库中随机抽取，排除已在5区域内显示的
- 头部描述改为显示"区域内X种 · 植物库共226种"
- `recommend-data.js` 不再被加载但保留文件

## 植物库扩展到500种
- 将 `plantDatabase` 从 226 种扩展到 **500 种**
- 新增 274 种植物覆盖10大类别：
  - 室内观叶(55): 竹芋类、龙血树类、秋海棠类、喜林芋类等
  - 多肉与仙人掌(50): 拟石莲属、景天属、十二卷属等
  - 阳台花卉(50): 美女樱、波斯菊、牡丹、铁线莲等
  - 蕨类(8): 肾蕨、蓝星蕨、纽扣蕨等
  - 香草食用(40): 九层塔、韭菜、黄瓜、茄子等
  - 水生植物(7): 荷花、菖蒲、梭鱼草等
  - 藤蔓攀援(9): 铁线莲、珊瑚藤、牵牛花等
  - 球根宿根(20): 剑兰、花毛茛、玉簪、鸢尾等
  - 灌木乔木(20): 茶花、玫瑰、樱花、日本枫等
  - 观赏草/其他(15): 紫穗狼尾草、彩叶草等
- 分类学依据 APG IV 体系，养护数据参考 RHS/AHS/《中国植物志》
- 验证结果：500种无重复，所有字段完整，默认27种不变
